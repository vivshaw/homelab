#cloud-config
autoinstall:
  version: 1

  # Locale settings
  locale: en_US
  keyboard:
    layout: us

  ssh:
    install-server: true
    allow-pw: true
    disable_root: true
    ssh_quiet_keygen: true
    allow_public_ssh_keys: true

  packages:
    - qemu-guest-agent
    - sudo

  storage:
    layout:
      name: direct
    swap:
      size: 0

  user-data:
    timezone: US/Eastern

    # On first boot, let's make sure we're updated!
    package_reboot_if_required: true
    package_update: true
    package_upgrade: true

    users:
      - name: foobar
        groups: [adm, sudo]
        lock-passwd: false
        sudo: ALL=(ALL) NOPASSWD:ALL
        shell: /bin/bash
        passwd: "$6$exDY1mhS4KUYCE/2$zmn9ToZwTKLhCw.b4/b.ZRTIZM30JZ4QrOQ2aOXJ8yk96xpcCof0kxKwuX1kqLG/ygbJ1f8wxED22bTL4F46P0"
        # TODO: Use key authorization instead of password
        # ssh_authorized_keys:
        #   - your-ssh-key
